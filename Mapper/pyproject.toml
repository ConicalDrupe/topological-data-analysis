[project]
name = "mapper"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "kmapper>=2.1.0",
    "pandas",
    "numpy",
    "scikit-learn",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mapper"]
